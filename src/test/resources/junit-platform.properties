# Auto-register the global Mockito session cleanup so a leaked inline/static
# mock from one test class can never poison later classes in the same fork
# (deterministic failures on GitHub Actions Temurin 17 otherwise).
junit.jupiter.extensions.autodetection.enabled=true
