apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: bhukkad

resources:
  - namespace.yaml
  - configmap.yaml
  - secrets.yaml
  - postgres/pvc.yaml
  - postgres/configmap.yaml
  - postgres/deployment.yaml
  - postgres/service.yaml
  - redis/configmap.yaml
  - redis/pvc.yaml
  - redis/deployment.yaml
  - redis/service.yaml
  # Redis Sentinel — enabled: the sentinel manifest now supports password auth
  # (sentinel auth-pass via REDIS_PASSWORD secret) so the three-replica sentinel
  # deployment can monitor the password-protected master. Requires the
  # bhukkad-redis service and redis-sentinel ConfigMap/deployment to be present.
  - redis/redis-sentinel.yaml
  # Secrets from Vault: the ExternalSecret materializes bhukkad-secrets from the
  # Vault SecretStore (see scripts/provision-vault.sh to seed the paths).
  - secret-store.yaml
  - external-secret.yaml
  - rabbitmq/deployment.yaml
  - app/serviceaccount.yaml
  - app/deployment.yaml
  - app/service.yaml
  - app/pdb.yaml
  - app/hpa.yaml
  - logging/fluent-bit-configmap.yaml
  - nginx/configmap.yaml
  - nginx/deployment.yaml
  - nginx/service.yaml
  - ingress.yaml
  - backup-scripts.yaml
  - backup-pvc.yaml
  - backup-cronjob.yaml

labels:
  - includeSelectors: true
    pairs:
      managed-by: kustomize
      project: bhukkad