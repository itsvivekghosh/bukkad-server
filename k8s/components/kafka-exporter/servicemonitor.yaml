apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: bhukkad-kafka-exporter
  namespace: bhukkad
  labels:
    app: bhukkad
    component: kafka-exporter
    # Prometheus Operator rule/monitor selector (same convention as
    # k8s/monitoring/*).
    release: bhukkad-prometheus
spec:
  namespaceSelector:
    matchNames:
      - bhukkad
  selector:
    matchLabels:
      app: bhukkad
      component: kafka-exporter
  endpoints:
    - port: metrics
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
      honorLabels: true
      relabelings:
        - sourceLabels: [__meta_kubernetes_pod_name]
          targetLabel: pod
        - sourceLabels: [__meta_kubernetes_service_name]
          targetLabel: service
        - sourceLabels: [__meta_kubernetes_namespace]
          targetLabel: namespace
