# EC2 public DNS endpoints (update when instances are replaced or get new Elastic IPs).
# Mirror these values in GitHub: Settings → Secrets and variables → Actions → Variables
STAGING_EC2_HOST=ec2-65-1-112-216.ap-south-1.compute.amazonaws.com
PROD_EC2_HOST=ec2-13-201-21-45.ap-south-1.compute.amazonaws.com
APP_PORT=8080
