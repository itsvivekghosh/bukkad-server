name: Feature CI

on:
  push:
    branches:
      - 'feature/**'

concurrency:
  group: feature-ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  ci:
    name: Build & Test
    uses: ./.github/workflows/ci.yml
