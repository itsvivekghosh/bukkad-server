spring:
  config:
    activate:
      on-profile: prod

  datasource:
    hikari:
      maximum-pool-size: ${IDENTITY_DB_POOL_SIZE:15}
      minimum-idle: ${IDENTITY_DB_POOL_SIZE:15}

app:
  environment: production