server:
  port: 8083

spring:
  application:
    name: order
  datasource:
    url: ${ORDER_DB_URL:jdbc:postgresql://localhost:5432/bhukkad_orders}
    username: ${ORDER_DB_USERNAME:bhukkad}
    password: ${ORDER_DB_PASSWORD:bhukkad_dev}
    hikari:
      maximum-pool-size: ${ORDER_DB_POOL_SIZE:30}
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration-pg

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      probes:
        enabled: true

logging:
  level:
    com.bhukkad: INFO

app:
  environment: production
  auth:
    jwt:
      # Per-service JWT validation (platform-lib PlatformJwtValidator). Empty
      # secret = auth filter inactive in dev (protected endpoints then 401);
      # set JWT_SECRET (shared HS256 secret) or JWKS_URL (RS256 via Keycloak)
      # to enable.
      secret: ${JWT_SECRET:}
      jwks-url: ${JWKS_URL:}
      issuer: ${JWT_ISSUER:}
      audience: ${JWT_AUDIENCE:}
  events:
    external:
      # Master switch for the Kafka publisher. When enabled and type=kafka,
      # the outbox poller also forwards events to Kafka (plan §6.2).
      enabled: false
      type: log
      kafka:
        bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
        consumer-group: ${KAFKA_CONSUMER_GROUP:order-platform-consumer}
        platform-topic: order.events.v1
        dlq-topic: order.events.v1.dlt
