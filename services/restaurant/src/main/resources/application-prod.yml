spring:
  config:
    activate:
      on-profile: prod

  datasource:
    hikari:
      maximum-pool-size: ${RESTAURANT_DB_POOL_SIZE:20}
      minimum-idle: ${RESTAURANT_DB_POOL_SIZE:20}

app:
  environment: production