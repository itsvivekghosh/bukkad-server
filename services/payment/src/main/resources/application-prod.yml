spring:
  config:
    activate:
      on-profile: prod

  datasource:
    hikari:
      maximum-pool-size: ${PAYMENT_DB_POOL_SIZE:20}
      minimum-idle: ${PAYMENT_DB_POOL_SIZE:20}

app:
  environment: production