# Reference Kubernetes manifests for identity (plan §11/P8).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: identity
  labels: {app: identity}
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate: {maxSurge: 1, maxUnavailable: 0}
  selector:
    matchLabels: {app: identity}
  template:
    metadata:
      labels: {app: identity}
    spec:
      containers:
        - name: identity
          image: ghcr.io/bhukkad/identity:latest
          imagePullPolicy: IfNotPresent
          ports: [{containerPort: 8080}]
          env:
            - {name: IDENTITY_DB_URL, value: jdbc:postgresql://postgres:5432/bhukkad_identity}
            - {name: IDENTITY_DB_USERNAME, valueFrom: {secretKeyRef: {name: db-secrets, key: username}}}
            - {name: IDENTITY_DB_PASSWORD, valueFrom: {secretKeyRef: {name: db-secrets, key: password}}}
          resources:
            requests: {cpu: 250m, memory: 512Mi}
            limits: {cpu: 1, memory: 1Gi}
          readinessProbe:
            httpGet: {path: /actuator/health/readiness, port: 8080}
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet: {path: /actuator/health/liveness, port: 8080}
            initialDelaySeconds: 30
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: identity
spec:
  selector: {app: identity}
  ports:
    - {port: 8080, targetPort: 8080}
