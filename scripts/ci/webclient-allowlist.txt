# scripts/ci/webclient-allowlist.txt
#
# G-13 grandfather list for hand-built HTTP clients (scripts/ci/check-http-clients.py).
# Format:
#   path/to/File.java                     — exempt the whole file
#   path/to/File.java:42                  — exempt one line
#   path/to/File.java:42:WebClient.builder()  — exempt one line + detector
# Blank lines and # comments are ignored.
#
# Every entry is a sibling-batch migration debt (PERF-5/P-05). The owning
# batch removes its lines when it migrates the file onto the platform
# WebClient factory (com.bhukkad.common.web.client.PlatformWebClientBuilderFactory).
#
# ── delivery service: OsrmRestTemplate bean (owner: W3-DELIVERY batch) ──────
services/delivery/src/main/java/com/bhukkad/delivery/OsrmHttpConfig.java
#
# ── platform-lib security (NOT web/client): JWKS fetch via RestClient ───────
# Owner: the security-package batch (W3-RESTRUCTURE owns security packaging).
services/platform-lib/src/main/java/com/bhukkad/common/security/PlatformJwtValidator.java
